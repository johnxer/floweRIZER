> **After pulling run:**  
> **`rm -rf .git`**  
> to remove the git repo connection.

> **For Firebase run:**
> **`firebase-init`**
> (npm install -g firebase-tools needs to be installed) 

Optional packages:
`"date-fns": "^4.1.0",` under "dependencies" - format the date + locale
